spring:
  application:
    name: contract-service
  jackson:
      serialization:
        write_dates_as_timestamps: false

server:
  port: 8081

logging:
  level:
    root: WARN
    company.tothepoint: DEBUG
---

spring:
  profiles: docker
  data:
    mongodb:
      host: contract-mongo
  rabbitmq:
      host: rabbitmq
app:
  rabbitmq:
      host: rabbitmq